apiVersion: v1
kind: Service
metadata:
  labels:
    app: pod-info
  name: pod-info
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: pod-info
  type: LoadBalancer
